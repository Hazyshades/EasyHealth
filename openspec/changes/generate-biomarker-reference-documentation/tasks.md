## 1. Reconcile candidate work and isolate technical corpus evidence

- [x] 1.1 Inventory and diff the uncommitted candidate generator, verifier, Wiki exporter, four docs, candidate issue document, README, package commands, and Registry CI changes. Record an adopt/revise/remove-or-relocate disposition for every file before editing it.
- [x] 1.2 Add `runRegistryV2CandidateCorpusTechnical` in `scripts/lib/registry-v2-candidate-corpus.ts`; make it derive row evidence, fixture validation, and thresholds without reading, parsing, hashing, validating, or exposing `approvals.json` or launchability.
- [x] 1.3 Change `scripts/registry-v2-candidate-corpus.ts --technical-check` and the docs renderer to use the technical API, while preserving the existing full approval-bound `runRegistryV2CandidateCorpus` release path.
- [x] 1.4 Add isolation tests proving technical corpus output/check behavior succeeds when production approvals are absent, unsigned, malformed, or hash-mismatched, while technical fixture/threshold failures still fail.

## 2. Public runtime inputs, shared Health Profile projection, and baseline

- [x] 2.1 Re-export `getRegistryV2ScoreRole`, `getRegistryV2ScoreReadinessGroups`, and `getRegistryV2ScoreContributionGroups` from `src/lib/biomarkers/index.ts`; preserve their current behavior from `registry-v2-runtime.ts`.
- [x] 2.2 Extract the inline laboratory-admission `flatMap` in `src/app/api/health-profile/route.ts` into pure `projectHealthProfileLaboratoryInput({ observation, relation, labUnitSystem })` at `src/lib/health-profile-input.ts`. It MUST return one exact `buildHealthProfile` input or `null`, retaining the route’s exact calls to `projectActiveRegistryV2LaboratoryBinding`, `projectLaboratoryOutcome`, reviewed-compatible binding selection, value-kind behavior, `presentObservation` conversion, raw unit/reference handling, and Health Profile input shape. Keep route authorization, queries, processed-document filtering, laboratory-only filtering, sources, `buildHealthProfile`, and response serialization unchanged.
- [x] 2.3 Add `registry/biomarker-registry/v2.0.0/documentation-baseline.json` with schema/version, exact runtime catalog manifest version and digest, derived catalog counts, alias locale counts, and technical corpus totals. Do not encode those facts as generator implementation constants.
- [x] 2.4 Add a typed deterministic renderer importing only the specified Registry public exports, `projectActiveRegistryV2LaboratoryBinding`, `projectLaboratoryOutcome`, `projectHealthProfileLaboratoryInput`, and the technical corpus API. Validate all inputs before writing: baseline count/version/digest reconciliation, unique definition/alias identities, per-alias ownership/governance metadata, and complete reviewed assessment-binding runtime metadata.
- [x] 2.5 Add explicit stable comparators and Markdown-cell escaping for definitions, alias groups, aliases, bindings, canonical eligibility fixtures, corpus rows, and links. Prohibit timestamps, local paths, Git metadata, network reads, GitHub API calls, and approval reads.

## 3. Canonical docs, persistence truth, and Wiki ownership

- [x] 3.1 Implement full ownership/write/check behavior for `docs/03-modules/biomarkers.md`, `docs/05-data/biomarker-catalog.md`, `docs/05-data/biomarker-aliases.md`, and `docs/05-data/biomarker-corpus-evidence.md`; add generated-file notices.
- [x] 3.2 Implement the exact one-pair README markers `<!-- generated-biomarker-docs:start -->` / `<!-- generated-biomarker-docs:end -->`. Update only their interior, preserve every byte outside them, and fail safely for missing, duplicate, or reversed markers.
- [x] 3.3 Render all definitions exactly once, explaining 77 analytes versus 107 definitions. Render reviewed and provisional sections separately, with identity axes, units/conversions/missing-unit behavior, provenance/lifecycle, compact EN/RU/ES aliases, and the shared Health Profile eligibility/score-role/readiness/contribution projection.
- [x] 3.4 Render the actual unknown/incomplete lifecycle: worker raw extraction persists to `document_extracted_biomarkers` before resolution; API preview is non-persistent; acceptance/correction/operator batch reprocessing persist each resolver result through the writer/RPC; `document_type` remains on `documents`; and automatic catalog promotion, review queue, source-document-to-catalog provenance promotion, automatic reprocessing, and HTTP batch administration are unavailable.
- [x] 3.5 Render technical corpus metrics and 72 stable evidence rows without approval, approval-hash, approval-error, or launchability data. Render unknown-marker rows as unmapped and explain no automatic catalog growth.
- [x] 3.6 Render every alias once in the full governance table. Derive compact catalog aliases and full alias rows from the same sorted alias model; use alias key as the join identity and render laboratory restrictions without broadening scope.
- [x] 3.7 Render the module lifecycle guide and legacy summary; link the authoritative v1 audit without duplicating 113 concepts.
- [x] 3.8 Replace the candidate `scripts/export-biomarker-wiki.ts` with a generated mirror exporter. `--render` must produce only the deterministic seven-page data set on stdout; `--write` must require an explicit local empty staging directory and write only `Home.md`, `Biomarker-Architecture.md`, `Biomarker-Catalog.md`, `Biomarker-Aliases.md`, `Biomarker-Corpus-Evidence.md`, `Registry-v1-Legacy.md`, and `_Sidebar.md`, derived solely from canonical generated docs and their canonical-doc-derived index.
- [x] 3.9 Reconcile `docs/05-data/biomarker-catalog-issue.md`: do not adopt it as generated output; preserve it until review, then remove or relocate it as the clean cutover from the candidate approach. Do not create or modify a GitHub issue or Wiki in this task.
- [x] 3.10 Implement exact package scripts: `generate:biomarker-docs`, `check:biomarker-docs`, `test:biomarker-docs`, `render:biomarker-docs-issue-body`, `render:biomarker-wiki`, and `export:biomarker-wiki`, with the specified side-effect behavior.

## 4. Verification and CI

- [x] 4.1 Add `test:biomarker-docs` tests proving byte-stable docs/Wiki/issue rendering; all 107 definition keys exactly once; 68/39 maturity counts; 625 aliases with en 277, ru 179, es 169; one full alias row per alias; and catalog-projection/full-table alias-key consistency.
- [x] 4.2 Add tests that read the baseline and assert its exact manifest version and digest equal `MEASUREMENT_CATALOG_MANIFEST_VERSION` and `MEASUREMENT_CATALOG_MANIFEST_DIGEST`, in addition to all derived count reconciliations.
- [x] 4.3 Extend `supabase/tests/writer_rpc_seam.sql` or add a sibling direct-RPC fixture covering `resolved`, `partial`, `ambiguous`, and `unmapped` with the exact JSON shapes sent by `buildObservationPayload` / `buildNormalizationResolutionPayload`. Assert each call creates or reuses exactly one source-linked observation, promotes exactly one active revision, preserves raw label/value/unit and document linkage, and projects the outcome/trace/status back to `document_extracted_biomarkers`; assert only `resolved` carries a concrete definition and `user_verified` acceptance, while incomplete outcomes have null definition and `pending` verification, with analyte identity allowed only when the resolver supplied it.
- [x] 4.4 Add a focused integration/contract test proving `worker/src/pipeline.ts` initial extraction inserts raw extracted fields with `status='needs_review'` before any writer/RPC call; `GET /api/documents/[id]/biomarkers` computes a non-persistent `buildNormalizationReview` preview; `GET /api/documents/[id]/observations` serializes the persisted raw/outcome fields; and `document_type` comes only from the joined `documents` record. Exercise acceptance, correction/confirmation, and Registry batch reprocessing as the only three writer entry families; assert no HTTP Registry-batch endpoint exists.
- [x] 4.5 Before deleting the inline route projection, characterize its output as literal expected Health Profile inputs for resolved, partial, ambiguous, unmapped, no-active-revision, trace-mismatch, non-lab, provisional, incompatible-binding, qualitative, numeric-conversion, and missing-reference fixtures. After extraction, table-test `projectHealthProfileLaboratoryInput` against those deep-equal expectations and assert the route’s resulting `buildHealthProfile` output remains unchanged. Then assert generated per-definition binding, score role, readiness/contribution groups, and consumer-eligibility claims equal the helper, `projectActiveRegistryV2LaboratoryBinding`, `projectLaboratoryOutcome`, and `buildHealthProfile`. Provisional definitions MUST remain ineligible and never affect a Health Profile under a synthetic resolved revision.
- [x] 4.6 Add negative tests proving invalid reviewed bindings, duplicate definition keys, duplicate alias keys, baseline count/version/digest mismatch, and invalid README markers fail before outputs are written.
- [x] 4.7 Add an isolated stale-file test proving `--check` lists every stale owned file or managed README section, exits non-zero, and never mutates stale or unrelated README content.
- [x] 4.8 Add Wiki tests proving `--render` and local staging are byte-stable, contain exactly the seven owned pages, are derived from canonical docs rather than hard-coded counts/approvals/runtime/corpus/issue data, and have no publication, network, credential, or filesystem side effect in render mode.
- [x] 4.9 Run `pnpm generate:biomarker-docs`; review the complete generated diff including persistence wording, verbatim EN/RU/ES aliases, Health Profile claims, provisional ineligibility, v1 links, unavailable-capability wording, and Wiki staging. Then verify `pnpm check:biomarker-docs`, `pnpm test:biomarker-docs`, and Wiki render/staging checks.
- [x] 4.10 Add the exact `pnpm check:biomarker-docs` and `pnpm test:biomarker-docs` steps to `.github/workflows/measurement-registry.yml` after `pnpm typecheck` and before cutover and broader Registry checks.

## 5. Separate issue and Wiki publication handoff

- [x] 5.1 Verify `pnpm render:biomarker-docs-issue-body` writes only deterministic concise index text to stdout and performs no filesystem or GitHub action.
- [x] 5.2 Verify `pnpm render:biomarker-wiki` produces only deterministic page data; verify `pnpm export:biomarker-wiki -- --output=<empty-local-staging-dir>` writes only the seven pages and never invokes Git or a network client.
- [x] 5.3 After all CI-equivalent checks are green, present the rendered issue body and local Wiki staging diff for explicit maintainer authorization. Only in separately authorized actions may a maintainer create/update the tracking issue and copy/review/publish the staged Wiki pages; do not paste catalog, alias, or corpus inventories.
