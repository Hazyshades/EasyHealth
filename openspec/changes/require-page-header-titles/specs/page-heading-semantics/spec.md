## ADDED Requirements

### Requirement: Main page headers identify their page
Every PageHeader SHALL have a visible title matching its navigation label and SHALL render that title as an h1.

#### Scenario: User enters a main application route
- **WHEN** the page content renders
- **THEN** it SHALL expose one visible h1 that identifies the page.