## 1. Editor project discovery

- [x] 1.1 Add repository-owned VS Code recommendations and settings for the application and document worker TypeScript projects.
- [x] 1.2 Add a developer guide describing the editor setup and compiler fallback.

## 2. Verification

- [x] 2.1 Add a root verification command that checks both TypeScript projects without emitting artifacts.
- [x] 2.2 Run the verification command and confirm each project reports independently.
- [x] 2.3 Validate the OpenSpec change in strict mode.